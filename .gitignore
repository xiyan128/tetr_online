# Build outputs
/target
/dist
/node_modules
/.wasm-dev
/dist-embed
/.embed-dev
/runs/

# OS / editor cruft
.DS_Store
.idea/

# Agent-internal planning & reference docs (roadmap, guideline, research notes).
# Kept locally for the agents working on the project; not part of the repo.
/agent
.claude/

# Python (uv-managed)
python/.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

# Machine-local RL-campaign model outputs (17GB of trained nets); the deployed
# weights live in models/ and Git LFS. Never commit this scratch dir.
/valuenet/

# Local agent-tooling state (skill manager)
.agents/
skills-lock.json
