//! Versus overlays: countdown, pause, and the result banner.
//!
//! Placeholder registration — the overlays land with the flow pass of the
//! versus strike (see `docs/adr-versus-mode-ui.md`, Decision 5).

use bevy::prelude::*;

pub struct VersusOverlayPlugin;

impl Plugin for VersusOverlayPlugin {
    fn build(&self, _app: &mut App) {}
}
